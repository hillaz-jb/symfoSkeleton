<main class="container">
    <h1 class="text-center my-4">{{ title|trans }}</h1>
    <div class="row mb-5">
        <div class="col-6 offset-3">
            {{ form_start(form) }}
            {{ form_end(form) }}
        </div>
    </div>
</main>